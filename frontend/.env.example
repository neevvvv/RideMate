# Backend API base URL — must match the backend PORT and host
VITE_BASE_URL=http://localhost:3001

# Google Maps API key — https://console.cloud.google.com/
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here
